# tenzing-agent-harness

Go module: `tenzing-agent`

## Architecture

`SYSTEM_ARCHITECTURE.md` is the source of truth for system design. When code changes affect the harness structure, agent interface, tool system, provider layer, or configuration surface, update the architecture doc in the same change. Do not let the doc drift from the code.
